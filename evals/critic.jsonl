{"id":"c-001-slop-rethink","why":"Classic AI slop: fabricated SLAs, taxonomy listing, no concrete problem. Critic must catch dimensions 1 (Correctness — no problem stated), 2 (fabricated SLA), 6 (slop), 7 (no Tests Plan).","input":{"problem":"Multi-agent system needs orchestration","design":"Add a comprehensive multi-agent orchestration framework with sequential / parallel / pipeline / hierarchical / map-reduce patterns. Performance target: 95% task completion rate, P99 < 5s, resource utilization 60-80%.","alternatives":"No alternatives stated.","constraints":"Production-grade, scalable.","mmcg_snapshot":"None provided."},"expect":{"verdict":"rethink","contains":["fabricated","slop"],"not_contains":["ship it"]}}
{"id":"c-002-clean-ship-with-caveats","why":"Clean design grounded in mmcg, mirrors existing pattern. Should not trigger rethink. May surface a perf concern (lock contention if hot-path) → ship or ship with caveats is OK. Note: critic may mention 'fabricated' in meta-discussion of dimension 6 even for clean designs — don't put it in not_contains.","input":{"problem":"Metrics exporters need to read in-memory session count without iterating the map.","design":"Add `pub fn session_count(&self) -> usize` to `SessionStore` impl block in `sdk/edge-ai-core/src/runtime/session.rs:302`. Mirrors the locking pattern of the existing `turn_count` accessor. Returns count of mapped entries.","alternatives":"1) Atomic counter mirror — rejected: adds write overhead on every insert/delete, snapshot-read is rare. 2) Iterate map on demand — rejected: O(N) where N grows unbounded.","constraints":"No public API changes elsewhere. Lock-pattern parity with existing accessors.","mmcg_snapshot":"mmcg_search SessionStore --language rust → 4 hits including impl at session.rs:302. mmcg_callers SessionStore → 45 callers. mmcg_search session_count → 0 hits (name available). Adjacent accessors turn_count, clarification_rounds_so_far follow read-lock pattern."},"expect":{"verdict":"ship","contains":["session_count"],"not_contains":["rethink"]}}
{"id":"c-003-missing-mmcg-fails-dim7","why":"Code-modifying design with no mmcg evidence — planner rule says critic flags dim 7 (Test & doc) as fail and dim 6 (slop) as concern/fail. Two fails → rethink; one fail → revise. Either acceptable, both are non-ship.","input":{"problem":"Add caching to the user session lookup.","design":"Wrap the session lookup function in an LRU cache with TTL 5 minutes.","alternatives":"1) No cache — rejected, current latency too high. 2) Redis cache — rejected, adds infra dependency.","constraints":"Single-process only for now.","mmcg_snapshot":"Not gathered."},"expect":{"verdict":["rethink","revise"],"contains":["mmcg"],"not_contains":["ship it"]}}
{"id":"c-004-single-alternative-concern","why":"Non-trivial design (multi-file refactor) with only 1 rejected alternative — planner mandate is ≥2. Critic should flag concern on dim 7 or recommend revising.","input":{"problem":"Refactor session cache to use LRU eviction with TTL.","design":"Replace HashMap with lru::LruCache across SessionStore, SessionPool, and SessionTracker. Add TTL evict thread.","alternatives":"1) Plain HashMap with periodic sweep — rejected: GC pauses.","constraints":"Multi-file change, public API of SessionStore changes.","mmcg_snapshot":"mmcg_search SessionStore → 4 hits. mmcg_callers SessionStore → 45. mmcg_impact SessionStore --depth 3 → 904 transitive."},"expect":{"verdict":"concern","contains":["alternative"],"not_contains":[]}}
{"id":"c-005-perf-no-observability","why":"Performance-critical change (hot-path metric emission) without observability plan — critic should flag dim 3 (Observability) as concern or fail. Either ship-with-caveats or revise is acceptable; the test is that the Observability dimension surfaces in the verdict, not the exact severity.","input":{"problem":"Add per-request latency metric to the inference engine entry point.","design":"Increment a Prometheus histogram on every request in `engine.generate()`. Use existing `prometheus_client` Histogram with buckets [0.01, 0.05, 0.1, 0.5, 1.0, 5.0].","alternatives":"1) Span attributes only — rejected: not aggregatable across hosts. 2) Custom metrics endpoint — rejected: duplicates Prometheus.","constraints":"Hot path (every request). Must not allocate per call.","mmcg_snapshot":"mmcg_search engine.generate → 1 hit at engine.py:142. mmcg_callers generate → 87 callers across api/ and tests/. mmcg_imports prometheus_client → already used at metrics.py:5."},"expect":{"verdict":["ship","revise"],"contains":["observability"],"not_contains":["rethink"]}}
