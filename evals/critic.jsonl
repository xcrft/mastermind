{"id":"c-001-slop-rethink","why":"Claims of measured success contradict supplied results, and synchronous execution violates the stated response contract. These are evidenced failures, not missing evidence.","input":{"problem":"Uploads must be acknowledged asynchronously within 2 seconds; processing can take 30 seconds.","design":"Replace the existing queue handoff with synchronous processing in the request handler. The proposal claims the trial measured 95% task completion and P99 below 5 seconds, so no failure monitoring is needed.","alternatives":"No alternatives stated.","constraints":"Preserve the 2-second acknowledgement contract.","mmcg_snapshot":"Source upload.py:40 enqueues before returning. Trial report trial.md:8 records 60% completion and P99 of 31 seconds. No other trial was run."},"expect":{"verdict":"rethink","contains":["fabricated"],"not_contains":["ship it"]}}
{"id":"c-002-clean-ship-with-caveats","why":"Clean design grounded in mmcg, mirrors existing pattern. Should not trigger rethink. May surface a perf concern (lock contention if hot-path) → ship it or ship with caveats is OK. Note: critic may mention 'fabricated' in meta-discussion of dimension 6 even for clean designs — don't put it in not_contains.","input":{"problem":"Metrics exporters need to read in-memory session count without iterating the map.","design":"Add `pub fn session_count(&self) -> usize` to `SessionStore` impl block in `sdk/edge-ai-core/src/runtime/session.rs:302`. Mirrors the locking pattern of the existing `turn_count` accessor. Returns count of mapped entries.","alternatives":"1) Atomic counter mirror — rejected: adds write overhead on every insert/delete, snapshot-read is rare. 2) Iterate map on demand — rejected: O(N) where N grows unbounded.","constraints":"No public API changes elsewhere. Lock-pattern parity with existing accessors.","mmcg_snapshot":"mmcg_search SessionStore --language rust → 4 hits including impl at session.rs:302. mmcg_callers SessionStore → 45 callers. mmcg_search session_count → 0 hits (name available). Adjacent accessors turn_count, clarification_rounds_so_far follow read-lock pattern."},"expect":{"verdict":["ship it","ship with caveats"],"contains":["session_count"],"not_contains":["rethink"]}}
{"id":"c-003-missing-evidence-abstains","why":"The session invalidation paths, baseline latency and correctness requirements are unknown; absent graph evidence alone is not a demonstrated design failure.","input":{"problem":"Add caching to the user session lookup.","design":"Wrap the session lookup function in an LRU cache with TTL 5 minutes.","alternatives":"1) No cache — rejected, current latency too high. 2) Redis cache — rejected, adds infra dependency.","constraints":"Single-process only for now.","mmcg_snapshot":"Not gathered."},"expect":{"verdict":"insufficient evidence","contains_any":[["invalidation","mutation","stale","freshness"]],"not_contains":["ship it"]}}
{"id":"c-004-single-plausible-alternative","why":"One genuine alternative must not create an invented second option or a failing review dimension.","input":{"problem":"Provide a private constant-time count accessor for a locked map.","design":"Add len() under the existing read lock. The source implementation has no iteration or allocation. Unit tests cover empty and populated maps.","alternatives":"One real alternative is an atomic mirror, rejected because all writes would need another synchronization invariant. No other live requirement justifies another design.","constraints":"Internal accessor only, no migration, not a hot path. Existing lock behavior and diagnostics are unchanged. API docs and tests are included.","mmcg_snapshot":"Source store.rs:20-33 shows len() and the identical lock pattern in is_empty(). Tests store_test.rs:4-22 assert zero and three entries. Only diagnostics.rs:10 consumes the new private accessor."},"expect":{"verdict":["ship it","ship with caveats"],"contains":["alternative"],"not_contains":["rethink","at least two alternatives are required"]}}
{"id":"c-005-unverified-allocation-contract","why":"A hard no-allocation constraint cannot be declared met from import and caller counts.","input":{"problem":"Add per-request latency metric to the inference engine entry point.","design":"Increment a Prometheus histogram on every request in `engine.generate()`. Use existing `prometheus_client` Histogram with buckets [0.01, 0.05, 0.1, 0.5, 1.0, 5.0].","alternatives":"1) Span attributes only — rejected: not aggregatable across hosts. 2) Custom metrics endpoint — rejected: duplicates Prometheus.","constraints":"Hot path (every request). Must not allocate per call.","mmcg_snapshot":"mmcg_search engine.generate → 1 hit at engine.py:142. mmcg_callers generate → 87 callers across api/ and tests/. mmcg_imports prometheus_client → already used at metrics.py:5. No implementation of Histogram.observe or allocation profile is supplied. Whether it allocates is unknown."},"expect":{"verdict":"insufficient evidence","contains_any":[["allocation","allocate"]],"not_contains":["rethink"]}}
{"id":"c-006-known-defect-survives-unknown","why":"An unmeasured performance effect cannot erase a proven compatibility defect.","input":{"problem":"Add metadata to the exported status response without breaking clients.","design":"Rename the existing required JSON field status to state and add optional age. Preserve all other behavior.","alternatives":"Keeping status while adding age is viable.","constraints":"Existing clients must continue to parse responses. Performance has not been measured.","mmcg_snapshot":"Contract status_schema.json:8 requires status. Source client.py:12 reads response[\"status\"] with no alias fallback. Proposed diff removes status. No runtime profile is provided."},"expect":{"verdict":["revise","rethink"],"contains":["status"],"not_contains":[]}}
{"id":"c-007-source-evidence-without-mmcg","why":"A graph outage does not block a fully source-grounded review of a small accessor.","input":{"problem":"Provide a private constant-time count accessor for a locked map.","design":"Add len() under the existing read lock. The source implementation has no iteration or allocation. Unit tests cover empty and populated maps.","alternatives":"Use the existing read-lock len() or an atomic mirror. The mirror adds an unnecessary synchronized copy.","constraints":"Internal accessor only, no migration, not a hot path. Existing lock behavior and diagnostics are unchanged. API docs and tests are included.","mmcg_snapshot":"mmcg is unavailable. Source inspection: store.rs:20-33 takes the established read lock and returns map.len(). diagnostics.rs:10 is the only consumer after a repository-wide source search. Tests store_test.rs:4-22 cover empty and populated maps and passed. No public API, persistence, error or hot-path behavior changes."},"expect":{"verdict":["ship it","ship with caveats"],"not_contains":["rethink","missing mmcg evidence is a fail"]}}
