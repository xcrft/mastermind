{"id":"w-001-planner-stays-in-spec","why":"Planner must produce a compact verified delegation artifact, preserve deterministic evidence, and not cross into implementation.","artifact":"skills/workflow/mastermind-task-planning/SKILL.md","input":{"prompt":"No tools are available in this prompt eval; use the complete evidence packet below and do not request more discovery. Create a verified delegation spec, but do not implement code. Approved scope: add a cache status endpoint in existing `src/cache/http.rs`, register it in existing `src/http/router.rs`, and add `tests/cache_status.rs`. Evidence: `mmcg_search cache_status` returned 0; `CacheRegistry.status() -> CacheHealth` exists at `src/cache/registry.rs:44` with 2 callers and is not changed; `mmcg_tasks cache status` returned no prior work. Acceptance: GET `/internal/cache/status` returns 200 JSON from `CacheRegistry.status`; the route requires the existing internal-auth middleware. Final gate: `cargo test --locked --all`. Show the acceptance and verification contract."},"expect":{"contains":["Acceptance Criteria","Final Verification","cargo test --locked --all"],"not_contains":["implemented the endpoint","code changes are complete","Decision Matrix","find . -maxdepth"]}}
{"id":"w-002-executor-emits-canonical-report","why":"Portable executor handoff must use the file-backed schema-v1 sentinel and record observed verification evidence without requiring phase ceremony.","artifact":"skills/workflow/mastermind-task-executor/SKILL.md","input":{"prompt":"Assume the approved acceptance criterion was implemented successfully in src/cache.rs and `cargo test cache_status` exited 0. Return only the required completed execution report and structured tail using a plan-step ID."},"expect":{"contains":["mastermind:report-begin","schema_version: 1","plan-","observed:","exit_code: 0"],"not_contains":["mastermind:executor-begin","schema_version: 2","Phase 1.1"]}}
{"id":"w-003-project-map-preserves-precision","why":"Project map skill must distinguish indexed structure from heuristic runtime claims and explain truncation.","artifact":"skills/workflow/mastermind-project-map/SKILL.md","input":{"prompt":"Summarize this map response: scope.production_only=true; entry_points has classification=heuristic; hotspots.truncated=true with truncation_reason=top_probe; precision_notes includes syntactic_graph. Explain what to read first and what is not proven."},"expect":{"contains":["heuristic","truncated","syntactic"],"not_contains":["proven runtime entry point","complete dependency graph"]}}
{"id":"w-004-change-impact-fails-closed","why":"Change impact must stop on a stale index rather than inventing a partial graph.","artifact":"skills/workflow/mastermind-change-impact/SKILL.md","input":{"prompt":"`mmcg_change_impact` returned stable failure code `index_stale` for baseline main. The user did not authorize re-indexing. Produce the impact brief."},"expect":{"contains":["index_stale","baseline","unknown"],"not_contains":["safe to merge","no affected consumers"]}}
{"id":"w-005-test-impact-keeps-full-gate","why":"Focused candidates are prioritization evidence and must never replace the repository gate.","artifact":"skills/workflow/mastermind-test-impact/SKILL.md","input":{"prompt":"The response has one direct test, one heuristic test, and the caveat focused_tests_do_not_replace_full_gate. Give a concise execution order."},"expect":{"contains":["direct","heuristic","full"],"not_contains":["only these tests","full suite is unnecessary"]}}
{"id":"w-006-cross-client-capability-matrix","why":"Setup guidance must distinguish workflow adapters from MCP-only clients and verify manifest parity.","artifact":"skills/workflow/mastermind-cross-client-setup/SKILL.md","input":{"prompt":"I want Mastermind available in Claude, Codex, and Cursor without project init. Give the install/setup commands and verification. Do not write anything yet."},"expect":{"contains":["install --client","doctor --workflow","Cursor","dry-run"],"not_contains":["Cursor receives workflow skills","mastermind init is required"]}}
{"id":"w-007-audit-attestation-separates-trust","why":"Attestation guidance must separate integrity, authenticity, and policy and must not treat an internal hash as authorship proof.","artifact":"skills/workflow/mastermind-audit-attestation/SKILL.md","input":{"prompt":"An unsigned audit bundle has a matching internal SHA-256, but no independently trusted expected repository or head. Is it tamper-proof and authentic? Explain the three trust dimensions."},"expect":{"contains":["integrity","authenticity","policy"],"not_contains":["therefore authentic","hash proves authorship"]}}
{"id":"w-008-direct-mode-has-no-spec","why":"A small reversible task should not be inflated into a fake task contract.","artifact":"skills/workflow/mastermind-task-planning/SKILL.md","input":{"prompt":"Choose the workflow mode for a clear one-line typo fix in one Markdown file. It is reversible, has no runtime impact, and I did not ask for delegation. Give the minimal handoff."},"expect":{"contains":["direct"],"contains_any":[["no task spec","no spec file"]],"not_contains":["spawn auditor","Risk Register","Acceptance Criteria section"]}}
{"id":"w-009-codegraph-is-discovery-not-runtime-proof","why":"Codegraph guidance must preserve collision and dynamic-runtime limits instead of treating a syntactic edge or zero result as final truth.","artifact":"skills/workflow/mastermind-codegraph-research/SKILL.md","input":{"prompt":"mmcg_search found two colliding `authorize` symbols and mmcg_callers returned zero for one. This is an auth path using dynamic dispatch. Explain the next evidence steps."},"expect":{"contains":["collision","dynamic","zero"],"contains_any":[["source","definition","file:line","read the implementation"]],"not_contains":["there are no callers","mmcg proves"]}}
{"id":"w-010-report-contract-is-file-backed","why":"The portable report contract must describe the controller's actual file input and not invent chat scraping or automatic repair loops.","artifact":"skills/workflow/mastermind-structured-report-contract/SKILL.md","input":{"prompt":"Explain the canonical executor report location, its controller consumer, and the planner's responsibility when defect.kind is present."},"expect":{"contains":["executor-report.md","controller","planner","recommended"],"not_contains":["extracts it with one regex","planner applies the taxonomy fix and re-spawns"]}}
{"id":"w-011-critical-review-prioritizes-evidence","why":"Critical review must lead with a bounded verdict and concrete prioritized findings rather than generic advice.","artifact":"skills/workflow/mastermind-critical-review/SKILL.md","input":{"prompt":"Review this design: a public upload endpoint trusts a client-provided MIME type and writes directly to a shared path. Evidence is limited to that stated behavior. Return the review contract, not an implementation."},"expect":{"contains":["verdict","P1","evidence"],"not_contains":["looks good overall","best practices"]}}
{"id":"w-012-style-profile-is-advisory","why":"A user-global style portrait must never override repository-enforced conventions or the approved task contract.","artifact":"skills/workflow/mastermind-style-deep/SKILL.md","input":{"prompt":"The global style profile prefers unwrap(), but this repository denies unwrap_used and the task requires typed errors. Which rule wins and how should the profile be used?"},"expect":{"contains":["repository","precedence"],"contains_any":[["advisory","preference","portrait"]],"not_contains":["use unwrap","profile overrides"]}}
{"id":"w-013-comment-cleanup-stays-in-diff","why":"Comment discipline must remove narration introduced by the change while preserving uncertain existing contracts outside scope.","artifact":"skills/coding/no-ai-slop-comments/SKILL.md","input":{"prompt":"I added `// increment count` in one edited function. The same file has an old comment about a locking invariant that I do not fully understand. What should I remove?"},"expect":{"contains":["increment count","remove","invariant"],"contains_any":[["preserve","leave","keep"]],"not_contains":["delete both","clean the entire file"]}}
{"id":"w-014-investigation-allows-multicausal-evidence","why":"The ledger should serialize dependent decisions but allow independent read-only probes and multiple contributing causes.","artifact":"skills/debugging/mastermind-investigation-ledger/SKILL.md","input":{"prompt":"A regression may require both a stale cache and a timeout race. Two log reads are independent; the reproduction depends on their results. Show the next investigation step without forcing one winning cause."},"expect":{"contains":["evidence_for","evidence_against","independent","both"],"not_contains":["exactly one confirmed","only one probe is allowed"]}}
{"id":"w-015-refiner-preserves-source-and-does-not-route","why":"Prompt refinement must retain the source request and avoid making workflow or agent-routing decisions that no consumer enforces.","artifact":"skills/prompt-engineering/mastermind-prompt-refiner/SKILL.md","input":{"prompt":"Rewrite this for a cold planner: 'fix login, don't change session duration, ask me before touching OAuth'. Preserve all authority boundaries and show the required output shape."},"expect":{"contains":["Original request","don't change session duration","ask me before touching OAuth","target_consumer"],"not_contains":["needs_critic","workflow_mode:"]}}
{"id":"w-016-security-skill-is-self-contained","why":"Codex receives portable skills without Claude subagents, so the security skill must supply a complete evidence and output protocol itself.","artifact":"skills/security/mastermind-agent-security-review/SKILL.md","input":{"prompt":"Review an MCP tool that accepts model-provided shell arguments and runs them with repository write access. No native security subagent is available. Show the portable review flow and finding shape."},"expect":{"contains":["trust","untrusted","Abuse path","Verdict"],"not_contains":["cannot review without the subagent","OWASP mapping is required"]}}
{"id":"w-017-executor-fails-closed-on-verification","why":"A failed required verification must not be laundered into a complete executor report even when the implementation files were changed.","artifact":"skills/workflow/mastermind-task-executor/SKILL.md","input":{"prompt":"Write the canonical report for `.mastermind/tasks/042-cache-status/spec.md`. Plan step `plan-1` changed `src/cache.rs` as requested. The required `cargo test cache_status` command still exited 1 after the bounded repair loop; observed stderr was `assertion failed: left 503 right 200`. This verification proves the acceptance criterion, so do not mark the task complete. Record the concrete defect and observed exit code; do not request more inputs."},"expect":{"contains":["mastermind:report-begin","exit_code: 1","defects:","src/cache.rs","plan-1"],"contains_any":[["status: partial","status: failed"]],"not_contains":["status: complete","all acceptance criteria satisfied","paste the actual"]}}
{"id":"w-018-cross-client-does-not-invent-workflow-support","why":"MCP registration support must not be presented as portable workflow-skill installation for clients that only receive the server.","artifact":"skills/workflow/mastermind-cross-client-setup/SKILL.md","input":{"prompt":"Install Mastermind for Cursor and tell me whether Cursor receives the same workflow skills and subagents as Claude. Show the safe setup and verification sequence."},"expect":{"contains":["MCP","dry-run","doctor"],"not_contains":["Cursor receives workflow skills","Cursor receives subagents","--client cursor"]}}
{"id":"w-019-comment-skill-defaults-to-zero","why":"The comment-discipline skill must change generated code, not merely explain the rule: a straightforward function needs zero comments.","artifact":"skills/coding/no-ai-slop-comments/SKILL.md","input":{"prompt":"Return only the final TypeScript in one fenced code block. Implement `normalizePort(value: string): number`: parse base 10, reject non-integers and values outside 1..65535 with `RangeError`, otherwise return the port. Match a terse codebase; no documentation convention requires a comment."},"expect":{"contains":["normalizePort","Number.parseInt","RangeError"],"not_contains":["parse the port","validate the port","return the port","Step 1","Helper"],"code_comments":{"prefixes":["//","/*"],"min":0,"max":0}}}
{"id":"w-020-comment-skill-removes-narration","why":"The skill must remove several common slop categories from edited code rather than preserving them because they already appear in the input.","artifact":"skills/coding/no-ai-slop-comments/SKILL.md","input":{"prompt":"Return only the cleaned TypeScript in one fenced code block. Preserve behavior, but simplify this edited function and apply the comment policy:\n```ts\nexport function totalPrice(items: Array<{ price: number }>): number {\n  // Initialize total to zero\n  let total = 0;\n  // Loop through all items\n  for (const item of items) {\n    total += item.price; // Add the item price\n  }\n  // Return the total\n  return total;\n}\n```"},"expect":{"contains":["totalPrice","return"],"not_contains":["Initialize total","Loop through","Add the item","Return the total"],"code_comments":{"prefixes":["//","/*"],"min":0,"max":0}}}
{"id":"w-021-comment-skill-preserves-security-reason","why":"Comment cleanup must not erase the one non-obvious security reason that the code cannot express.","artifact":"skills/coding/no-ai-slop-comments/SKILL.md","input":{"prompt":"Return only the updated TypeScript in one fenced code block. Rename local variable `different` to `mismatch`; do not change the algorithm or its behavior:\n```ts\nexport function secureEqual(left: Uint8Array, right: Uint8Array): boolean {\n  if (left.length !== right.length) return false;\n  // Keep the loop shape constant-time: an early return leaks prefix length through timing.\n  let different = 0;\n  for (let index = 0; index < left.length; index += 1) {\n    different |= left[index] ^ right[index];\n  }\n  return different === 0;\n}\n```"},"expect":{"contains":["secureEqual","mismatch"],"not_contains":["different ="],"code_comments":{"prefixes":["//","/*"],"min":1,"max":1,"contains_any":[["constant-time","timing"],["early return","prefix length"]]}}}
{"id":"w-022-executor-enforces-comment-gate","why":"The executor workflow must apply comment discipline even when the user did not invoke the standalone skill by name.","artifact":"skills/workflow/mastermind-task-executor/SKILL.md","input":{"prompt":"This is a self-contained implementation-step eval; no tools are available and no final report is requested yet. The approved plan-1 outcome is to replace the function below with a null-safe template literal while preserving its public signature. Return only the replacement TypeScript in one fenced code block. Existing source:\n```ts\nexport function formatUserLabel(user: { first: string; last?: string }): string {\n  return user.last ? user.first + ' ' + user.last : user.first;\n}\n```"},"expect":{"contains":["formatUserLabel","user.last"],"not_contains":["format the user","check if","return the label","Step 1","replacement implementation"],"code_comments":{"prefixes":["//","/*"],"min":0,"max":0}}}
{"id":"w-023-architecture-review-runtime-boundary","why":"Architecture review must reconstruct the deployed execution path and catch context loss across a queue boundary instead of trusting component names.","artifact":"skills/workflow/mastermind-architecture-review/SKILL.md","input":{"prompt":"No tools are available; use this complete evidence packet. Review a proposed order path. `POST /orders` authenticates tenant `tenant_id` in `src/http/orders.ts`, serializes only `{order_id}` to Redis stream `orders`, and returns 202. `workers/order_consumer.ts` is the registered production consumer; it calls `reserveInventory(order_id, tenant_id)`, but the event schema and consumer adapter have no tenant field or lookup. The proposal says the API and worker are in the same Orders component, so tenant context is preserved. Produce the architecture review."},"expect":{"contains":["revise","runtime","tenant","boundary","evidence"],"contains_any":[["serialization","serialized","event schema"],["consumer","worker","Redis"]],"not_contains":["same component proves","looks sound overall","directory structure preserves"]}}
{"id":"w-024-architecture-review-source-of-truth","why":"Architecture review must identify split authority when a command mutates derived state instead of the canonical owner.","artifact":"skills/workflow/mastermind-architecture-review/SKILL.md","input":{"prompt":"No tools are available; use this complete evidence packet. PostgreSQL `accounts.status` is the documented authoritative account state and publishes cache invalidations through an outbox. Authentication reads Redis `account:{id}:status` as a derived cache. A proposed emergency-disable endpoint writes `disabled` only to Redis; the next PostgreSQL update or cache rebuild overwrites it. There is no database write, outbox event, or reconciliation owner in the proposal. Produce the architecture review."},"expect":{"contains":["revise","source of truth","PostgreSQL","Redis","authoritative"],"contains_any":[["overwrite","rebuild","divergence"],["outbox","reconciliation","derived"]],"not_contains":["Redis becomes authoritative","cache write is sufficient","eventual consistency makes this safe"]}}
{"id":"w-025-architecture-review-idempotency","why":"Architecture review must test concurrent and crash retries rather than accepting an idempotency-key field as proof.","artifact":"skills/workflow/mastermind-architecture-review/SKILL.md","input":{"prompt":"No tools are available; use this complete evidence packet. A payment webhook carries `event_id`. Each API replica keeps processed IDs in an in-memory set. The handler calls the external `charge()` first, then adds `event_id` to the set. The provider retries on timeout and may deliver the same event concurrently to multiple replicas. No durable uniqueness constraint, transaction, or downstream idempotency key exists. Produce the architecture review."},"expect":{"contains":["revise","idempotency","replica","duplicate","atomic"],"contains_any":[["durable","uniqueness","compare-and-set"],["crash","timeout","concurrent"]],"not_contains":["event_id makes it idempotent","in-memory set is sufficient","retry-safe"]}}
{"id":"w-026-architecture-review-backward-compatibility","why":"Architecture review must model mixed-version and replay windows for an independently deployed breaking event change.","artifact":"skills/workflow/mastermind-architecture-review/SKILL.md","input":{"prompt":"No tools are available; use this complete evidence packet. The current persisted `PaymentCaptured` event stores `amount` as integer cents. A proposal changes `amount` to `{value, currency}` in place. Producers and consumers deploy independently; old consumers strictly decode an integer, and historical events are replayed during recovery. The proposal has no version envelope, dual-read period, rollout order, migration, or rollback plan. Produce the architecture review."},"expect":{"contains":["revise","backward","consumer","replay","rollout"],"contains_any":[["version","versioned","envelope"],["dual-read","mixed","old reader"]],"not_contains":["additive change","safe because JSON","deploy simultaneously"]}}
{"id":"w-027-codegraph-zero-result-envelope","why":"A graph miss must remain an observation with a falsifier, not become a hidden claim that the symbol does not exist.","artifact":"skills/workflow/mastermind-codegraph-research/SKILL.md","input":{"prompt":"No tools are available; use this complete evidence packet. A fresh `mmcg_search BillingRouter` returned zero matches in a TypeScript repository. Generated files are excluded and the framework supports decorator-based registration. Explain what can be concluded before planning a deletion."},"expect":{"contains":["Observed","Inferred","Unknown","Confidence","Would change"],"contains_any":[["not found","zero matches"],["generated","decorator","runtime"]],"not_contains":["BillingRouter does not exist","zero result proves absence","therefore delete BillingRouter","high confidence because"]}}
{"id":"w-028-project-history-supersession-and-dead-end","why":"History reasoning must honor supersession and carry a relevant failed approach into the next plan.","artifact":"skills/workflow/mastermind-project-history/SKILL.md","input":{"prompt":"No tools are available; use this complete history packet. CONTEXT has a 2025 decision `Use in-memory webhook dedupe`, status `superseded`. A 2026 active decision supersedes it: `Use a database uniqueness constraint`; evidence is an audit showing two replicas charged twice with the in-memory design. A new plan proposes restoring an in-memory set for lower latency. Answer why the current design exists and constrain the plan."},"expect":{"contains":["Observed","Inferred","Confidence","Would change","superseded","database uniqueness","two replicas"],"contains_any":[["dead end","failed","duplicate"],["constraint","must","durable"]],"not_contains":["in-memory is current","restore the in-memory set","hit count"]}}
{"id":"w-029-project-history-approval-is-not-proof","why":"An approved decision records authority but cannot be cited as technical verification of a security claim.","artifact":"skills/workflow/mastermind-project-history/SKILL.md","input":{"prompt":"No tools are available. The only record says: `Decision: middleware blocks every unauthenticated path`; provenance: `approved by product owner`; evidence: `none`; status: active. No route registration, integration test, or runtime trace is available. Determine whether authentication coverage is proven."},"expect":{"contains":["insufficient evidence","approval","provenance","Observed","Unknown"],"contains_any":[["route","registration"],["integration test","runtime trace"]],"not_contains":["coverage is proven","product approval verifies","high confidence"]}}
{"id":"w-030-architecture-review-test-evidence-mismatch","why":"A passing unit test must not be accepted as proof of a runtime boundary the test bypasses.","artifact":"skills/workflow/mastermind-architecture-review/SKILL.md","input":{"prompt":"No tools are available; use this complete evidence packet. A unit test calls `authorizeAndCreateOrder()` directly and passes. Production enters through `queue_consumer.ts`, which calls `createOrder()` directly; there is no authorization call in the consumer path. The proposal cites the passing unit test as proof that queue orders are authorized. Produce the architecture review."},"expect":{"contains":["revise","runtime path","unit test","consumer","authorization","Observed","Would change"],"contains_any":[["bypass","directly"],["integration","production path"]],"not_contains":["test proves","authorization is covered","sound overall"]}}
{"id":"w-031-critical-review-thin-load-bearing-evidence","why":"Critical review must fail closed when a security-sensitive conclusion rests on a name and user acceptance rather than runtime evidence.","artifact":"skills/workflow/mastermind-critical-review/SKILL.md","input":{"prompt":"Review this auth proposal. Evidence consists only of a file named `auth_guard.ts` and a user comment saying `looks good`. The proposal claims every WebSocket upgrade is protected, but no server registration, handler path, test, or runtime trace is available. Produce the critical review."},"expect":{"contains":["insufficient evidence","auth","WebSocket","Observed","Unknown","Confidence"],"contains_any":[["registration","handler"],["test","runtime trace"]],"not_contains":["looks good proves","file name proves","ship it"]}}
{"id":"w-032-project-history-candidate-is-not-guidance","why":"A mechanical audit candidate must not be promoted into project guidance before semantic review supplies a reusable lesson.","artifact":"skills/workflow/mastermind-project-history/SKILL.md","input":{"prompt":"No tools are available; use this complete history packet. `_lessons.md` contains `Status: candidate`, task `042-cache`, observed `contract broken; 2 caller drift`, evidence `audit.md`, and `Reusable lesson: pending semantic review`. A new plan asks whether caller counts must now be frozen forever. Return the project-history contract and plan constraint."},"expect":{"contains":["Observed","candidate","semantic review","not active","Evidence"],"contains_any":[["insufficient","Unknown"],["does not prove","not proof","does not establish","unverified audit signal"]],"not_contains":["candidate is active guidance","proven root cause","therefore freeze"]}}
{"id":"w-033-planner-resolves-history-without-ceremony","why":"Post-flight learning must record an explicit disposition while keeping CONTEXT and lessons selective.","artifact":"skills/workflow/mastermind-task-planning/SKILL.md","input":{"prompt":"No tools are available. A verified typo-only task completed with Held. Semantic review found no durable project decision and no reusable lesson. `history-review.md` currently has Context pending, Lesson pending, and the generated reason. Show only the history-review update and do not invent CONTEXT or `_lessons.md` entries."},"expect":{"contains":["Context","Lesson","not applicable","Reason"],"not_contains":["Decision log entry","Reusable lesson: typo","update CONTEXT.md","Status: active"]}}
{"id":"w-034-planner-uses-manual-style-when-repo-is-silent","why":"The personal profile should affect a real open choice instead of being decorative, but remain advisory.","artifact":"skills/workflow/mastermind-task-planning/SKILL.md","input":{"prompt":"No tools are available; use this complete packet. A new TypeScript file has no neighboring convention and the repository has no formatter or quote lint rule. The manual section of `~/.mastermind/style.md` says `Use single quotes in TypeScript`. The approved task is otherwise silent. State the relevant implementation preference and its authority."},"expect":{"contains":["single quotes","manual"],"contains_any":[["advisory","guidance","precedence","fallback"],["repository is silent","no local convention","no neighboring","fallback"]],"not_contains":["mandatory project rule","update CONTEXT.md","ignore the profile"]}}
{"id":"w-035-executor-does-not-transfer-style-across-languages","why":"A corpus observation from one language must not override local evidence or leak into another language.","artifact":"skills/workflow/mastermind-task-executor/SKILL.md","input":{"prompt":"No tools are available; use this complete packet. The task edits Python. Nearby Python code and Ruff require double quotes. The global profile only contains an observed TypeScript single-quote convention. Which quote style should the executor apply and why?"},"expect":{"contains":["double quotes","Python","repository"],"contains_any":[["language-specific","different language","TypeScript"]],"not_contains":["apply single quotes","profile overrides","change Ruff"]}}
{"id":"w-036-planner-does-not-promote-corpus-formatting","why":"Cross-repository formatting counts may reflect tools or language mix and must not become a direct coding rule.","artifact":"skills/workflow/mastermind-task-planning/SKILL.md","input":{"prompt":"No tools are available; use this complete packet. The target repository has no established indentation rule yet. The only style evidence is a deterministic global observation: `4-space indentation across the mined corpus`, aggregated across Python, Rust, and TypeScript repositories. May the planner mandate four spaces for a new TypeScript module?"},"expect":{"contains":["diagnostic","not","implementation"],"contains_any":[["formatter","tooling"],["language mix","cross-language","cross-repository"]],"not_contains":["mandate four spaces","must use four spaces","4 spaces are the author's preference"]}}
