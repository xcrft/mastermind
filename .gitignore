# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store

# Python (in case validation scripts get added)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node (in case any tooling needs it)
node_modules/
npm-debug.log*

# Local overrides
.env
.env.local
*.local

# Build/scratch
dist/
build/
tmp/
scratch/

# Mastermind local state — task specs, mmcg index, anything generated by `mmcg init`.
# Specs and the codegraph are working files, not committed artifacts.
.mastermind/

# Claude Code local state — per-machine permissions allowlist with absolute paths.
# Never share or commit; the file is regenerated per checkout.
.claude/settings.local.json
research/
